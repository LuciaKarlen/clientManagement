{
	"name": "client-management-server",
	"version": "1.0.0",
	"description": "Backend API for client management (Prisma + MySQL + Express)",
	"main": "index.js",
	"scripts": {
		"dev": "nodemon index.js",
		"start": "node index.js",
		"prisma:generate": "prisma generate",
		"prisma:migrate": "prisma migrate dev --name init"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.0.0",
		"express": "^4.18.2",
		"@prisma/client": "^5.10.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.22",
		"prisma": "^5.10.0"
	}
}

